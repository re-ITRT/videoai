请根据以下信息生成短视频剧本：

## 产品信息
- 产品名称：{{ product_info.name }}
- 产品描述：{{ product_info.description }}
- 核心卖点：{{ product_info.selling_points | join('、') }}

## 风格定位
{{ style }}

## 总时长
{{ duration }}秒

## 可用素材
{% for material in materials %}
- 素材ID {{ material.material_id }}: {{ material.text_content }}（标签：{{ material.tags | join('、') }}）
{% endfor %}

## 要求
1. 所有场景时长总和必须等于{{ duration }}秒
2. 每个场景用 `lines` 数组控制台词时间轴
3. lines 中每条台词需指定：speaker（说话人）、text（台词）、tone（语气语调）、start_sec/end_sec（在场景内的起止秒数）
4. 每个 scene 用 materials 数组引用可用素材ID，指明用到哪些素材
5. 至少有一些旁白台词，也可以加入角色对话增加代入感
6. 台词时长和场景时长相匹配
